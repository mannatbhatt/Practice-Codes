f=open("STORY.txt","r")
p=list(f)
def count(n):
    length = len(n)
    print(length)
    return length
count(p)

#text_file = input("Enter the lines ")
#q=list(text_file)
#def count(m):
    lines = len(m)
    print(lines)
    return lines
count(q)
